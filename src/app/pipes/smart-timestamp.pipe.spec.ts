import { SmartTimestampPipe } from './smart-timestamp.pipe';

describe('SmartTimestampPipe', () => {
  it('create an instance', () => {
    const pipe = new SmartTimestampPipe();
    expect(pipe).toBeTruthy();
  });
});
